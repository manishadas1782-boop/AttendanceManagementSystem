# Copy this file to config.properties and adjust for your environment
# MySQL connection example
# For MySQL 8+: ensure serverTimezone and proper SSL flags as needed

db.url=jdbc:mysql://localhost:3306/attendance_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.user=attendance_user
db.password=change_me
